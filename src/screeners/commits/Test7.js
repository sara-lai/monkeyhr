import * as GithubService from '../../services/GithubService'

// this is meant to be a fun test to try out binary search!
// https://frontendmasters.com/courses/algorithms/
// using the compare endpoint can determine if first half have the large commit or second half, and so on, splitting in half

function test7(repo){

  

  return {
    'testDescription': 'one very large commit among small ones',
    'resultFlag': '',
    'resultDescription': '',
  }
}

export default test7
