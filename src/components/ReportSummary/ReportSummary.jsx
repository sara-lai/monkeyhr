const ReportSummary = (props) => {

    // need to generate report summary cards here

    return (
        <>
            <div className='report-summary'>
                airtable id: {props.report.id}
            </div>
        </>
    )

}

export default ReportSummary